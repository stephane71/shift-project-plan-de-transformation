const styles = theme => ({
  navHeader: {
    display: "flex",
    alignItems: "center",
    justifyContent: "flex-start",
    padding: theme.spacing(2),
    cursor: "pointer"
  },
  homeIcon: {
    marginRight: theme.spacing(2)
  }
});

export default styles;
