const styles = theme => ({
  home: {},

  about: {
    marginTop: theme.spacing(5)
  },

  main: {
    display: "flex"
  },

  presentation: {
    padding: theme.spacing(2),
    paddingTop: 0
  }
});

export default styles;
