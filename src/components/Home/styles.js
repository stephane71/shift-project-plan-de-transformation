const styles = theme => ({
  home: {},

  aboutHeader: {
    marginTop: theme.spacing(5),
    marginBottom: theme.spacing(2)
  },

  main: {
    display: "flex"
  },

  presentation: {
    padding: theme.spacing(2),
    paddingTop: 0
  }
});

export default styles;
